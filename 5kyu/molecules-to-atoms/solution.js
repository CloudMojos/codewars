function parseMolecule(formula) {
    let atoms = {}
    let state = []
    for (let i = 0; i < formula.length; i++) {
        // If it starts with uppercase
        if (/A-Z/)
        // If it is a number

        // If it is an opening bracker

        // If it is a closing bracket

        // If it is any other character, ie. small letters
    }
    return atoms
}


const arg = 'Mg2O'
const x = parseMolecule(arg)
console.log(x)